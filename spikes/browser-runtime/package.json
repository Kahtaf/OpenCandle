{
  "name": "@opencandle/browser-runtime-spike",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:runtime": "node scripts/build-runtime-payload.mjs",
    "build": "npm run build:runtime && vite build",
    "dev": "npm run build:runtime && vite",
    "preview": "vite preview",
    "test": "vitest run",
    "test:browser": "npm run build && node tests/browser-runtime.e2e.mjs"
  },
  "devDependencies": {
    "@webcontainer/api": "1.6.4",
    "esbuild": "0.28.1",
    "playwright-core": "1.58.2",
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.0.18"
  }
}
